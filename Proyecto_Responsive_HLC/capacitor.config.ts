import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Proyecto_Responsive_HLC',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
